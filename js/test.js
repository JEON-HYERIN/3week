// 로드 후 실행
$(function() {

  // $('.name').hide().show();
  // $('.name').fadeIn().fadeOut();

  // $('.name').text('1111');
  // const name = $('.name').text();

  // $('.name').html(`<h1>${name}</h1>`);
  // $('.name').css('display', 'none');

  // $('.name').css('','');
  // $('.name').css({width: 100, height: 200});
  // $('.name').animate({width: 300}, 1000)
  //   .animate({height: 300}, 1000, function(){
  //   console.log(111);
  // });

  // $('.name').addClass('add').removeClass('add');
  // has = $('.name').hasClass('add');
  // console.log(has);

  // if(조건: 참) {
  //   실행
  // } else { 그렇지않다면
  //   실행
  // }

  // has = ($('.name').hasClass('add')) ? console.log('있어요') : console.log('없어용');

  // x
  // 'asdasd'
  // 123,12,3,
  // arr = [''];

  // human = {
  //   age : 67,
  //   name : '김유잠',
  //   child : ['유마', '자다', '자바', '바바']
  // }

  // w = $('.name').width();
  // w = $('.name').innerWidth();
  // w = $('.name').outerWidth();
  // console.log(w);

  // $('.list').append('<li>2</li>');
  // $('.list').remove();

  // $('.name').data('name');
  // $('.name').attr('class');
})
